#pragma once

#include <string>
#include <fstream>

class lexer {
    std::ifstream file;
    std::string lex;

public:
//    lexer(std::string seq);

//    std::string get_lex();

};

#include <iostream>
void process(const std::string &seq);